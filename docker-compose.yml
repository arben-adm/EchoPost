services:
  echopost:
    build: .
    ports:
      - "3010:3010"
    volumes:
      - echopost_data:/data
    environment:
      - DB_PATH=/data/echopost.db
      # - APP_PASSWORD=yourpassword
      # - APP_SECRET=change-this-to-a-random-64-char-string

volumes:
  echopost_data:
