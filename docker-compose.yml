services:
  voicedesk:
    build: .
    ports:
      - "3010:3010"
    volumes:
      - voicedesk_data:/data
    environment:
      - DB_PATH=/data/voicedesk.db
      # - APP_PASSWORD=yourpassword
      # - APP_SECRET=change-this-to-a-random-64-char-string

volumes:
  voicedesk_data:
